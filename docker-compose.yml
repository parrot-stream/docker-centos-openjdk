version: "3"

services:

     centos-openjdk:
        container_name: centos-openjdk
        build:
            context: .
            dockerfile: Dockerfile
        image: parrot-stream/centos-openjdk:7
